import 'package:flutter/material.dart';

class SearchOne extends StatelessWidget {
  const SearchOne({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Scaffold(body: SafeArea(child: Container(),),);
  }
}
